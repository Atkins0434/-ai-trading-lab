{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "end_of_day_outcome.schema.json",
  "title": "End of Day Outcome",
  "description": "Observed intraday outcome and deterministic execution result for a Scout candidate during historical replay.",
  "type": "object",
  "additionalProperties": false,

  "required": [
    "replay_id",
    "trading_date",
    "scout_version",
    "execution_policy_version",
    "massive_plan",
    "universe_mode",
    "universe_manifest_hash",
    "universe_coverage",
    "research_evidence",
    "promotion_eligible",
    "outcomes",
    "policy_comparisons"
  ],

  "properties": {
    "replay_id": {
      "type": "string"
    },

    "trading_date": {
      "type": "string",
      "format": "date"
    },

    "scout_version": {
      "type": "string"
    },

    "execution_policy_version": {
      "type": "string"
    },

    "massive_plan": {"$ref": "#/$defs/massivePlan"},

    "universe_mode": {"enum": ["ci_fixture", "historical_research"]},
    "universe_manifest_hash": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
    "universe_coverage": {"enum": ["complete", "incomplete", "fixture"]},
    "research_evidence": {"type": "boolean"},
    "promotion_eligible": {"type": "boolean"},

    "outcomes": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/securityOutcome"
      }
    },

    "policy_comparisons": {
      "type": "array",
      "items": {"$ref": "#/$defs/policyComparison"}
    }
  },

  "$defs": {
    "massivePlan": {
      "type": "object",
      "additionalProperties": false,
      "required": ["plan", "rest_calls_per_minute", "history_years", "flat_files"],
      "properties": {
        "plan": {"type": "string", "minLength": 1},
        "rest_calls_per_minute": {"type": ["number", "null"], "exclusiveMinimum": 0},
        "history_years": {"type": "integer", "minimum": 1},
        "flat_files": {"type": "boolean"}
      }
    },
    "securityOutcome": {
      "type": "object",
      "additionalProperties": false,

      "required": [
        "ticker",
        "selected",
        "selection_basis",
        "intraday_path",
        "mfe_pct",
        "mae_pct",
        "execution_result"
      ],

      "properties": {
        "ticker": {
          "type": "string",
          "minLength": 1
        },

        "selected": {
          "type": "boolean"
        },

        "selection_basis": {
          "enum": [
            "QUALIFYING_THRESHOLD",
            "EXPLORATION_TOP_K",
            "NOT_SELECTED"
          ]
        },

        "scout_rank": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 1
        },

        "scout_score_pct": {
          "type": [
            "number",
            "null"
          ],
          "minimum": 0,
          "maximum": 100
        },

        "intraday_path": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/bar"
          }
        },

        "mfe_pct": {
          "type": "number"
        },

        "mae_pct": {
          "type": "number"
        },

        "maximum_capturable_move_pct": {
          "type": [
            "number",
            "null"
          ]
        },

        "execution_result": {
          "$ref": "#/$defs/executionResult"
        }
      }
    },

    "bar": {
      "type": "object",
      "additionalProperties": false,

      "required": [
        "timestamp",
        "open",
        "high",
        "low",
        "close",
        "volume"
      ],

      "properties": {
        "timestamp": {
          "type": "string",
          "format": "date-time"
        },

        "open": {
          "type": "number",
          "minimum": 0
        },

        "high": {
          "type": "number",
          "minimum": 0
        },

        "low": {
          "type": "number",
          "minimum": 0
        },

        "close": {
          "type": "number",
          "minimum": 0
        },

        "volume": {
          "type": "number",
          "minimum": 0
        },

        "session": {
          "type": "string"
        },

        "source": {
          "type": "string"
        }
      }
    },

    "executionResult": {
      "type": "object",
      "additionalProperties": false,

      "required": [
        "trade_executed",
        "realized_return_pct",
        "policy_id",
        "exit_mode",
        "sizing_mode",
        "stop_distance_usd",
        "stop_distance_pct",
        "target_distance_usd",
        "target_distance_pct",
        "risk_usd_at_entry",
        "entry_rejection_reason"
      ],

      "properties": {
        "trade_executed": {
          "type": "boolean"
        },

        "entry_timestamp": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },

        "entry_price": {
          "type": [
            "number",
            "null"
          ],
          "minimum": 0
        },

        "position_size_shares": {
          "type": [
            "number",
            "null"
          ],
          "minimum": 0
        },

        "position_value_usd": {
          "type": [
            "number",
            "null"
          ],
          "minimum": 0
        },

        "exit_timestamp": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },

        "exit_price": {
          "type": [
            "number",
            "null"
          ],
          "minimum": 0
        },

        "exit_reason": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "ABNORMAL_PUT_VOLUME",
            "TRAILING_STOP",
            "PROFIT_TARGET",
            "MIDDAY_LIQUIDATION",
            "SESSION_END",
            "ENTRY_REJECTED",
            "NO_REGULAR_SESSION_PATH",
            null
          ]
        },

        "realized_pnl_usd": {
          "type": [
            "number",
            "null"
          ]
        },

        "realized_return_pct": {
          "type": "number"
        },

        "capture_ratio": {
          "type": [
            "number",
            "null"
          ]
        },

        "maximum_position_drawdown_pct": {
          "type": [
            "number",
            "null"
          ]
        },

        "policy_id": {"type": "string", "minLength": 1},
        "exit_mode": {"enum": ["PERCENT", "ATR"]},
        "sizing_mode": {"enum": ["FIXED_FRACTION", "RISK_PER_TRADE"]},
        "stop_distance_usd": {"type": ["number", "null"], "minimum": 0},
        "stop_distance_pct": {"type": ["number", "null"], "minimum": 0},
        "target_distance_usd": {"type": ["number", "null"], "minimum": 0},
        "target_distance_pct": {"type": ["number", "null"], "minimum": 0},
        "risk_usd_at_entry": {"type": ["number", "null"], "minimum": 0},
        "entry_rejection_reason": {
          "type": ["string", "null"],
          "enum": [
            "ATR_UNAVAILABLE",
            "POSITION_TOO_SMALL",
            "MAX_POSITIONS_REACHED",
            null
          ]
        }
      }
    },

    "policyComparison": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "policy_id", "exit_mode", "sizing_mode", "executions", "summary"
      ],
      "properties": {
        "policy_id": {"type": "string", "minLength": 1},
        "exit_mode": {"enum": ["PERCENT", "ATR"]},
        "sizing_mode": {"enum": ["FIXED_FRACTION", "RISK_PER_TRADE"]},
        "executions": {
          "type": "array",
          "items": {"$ref": "#/$defs/comparisonExecution"}
        },
        "summary": {"$ref": "#/$defs/policySummary"}
      }
    },

    "comparisonExecution": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "ticker", "cohort", "benchmark_rank", "execution_result"
      ],
      "properties": {
        "ticker": {"type": "string", "minLength": 1},
        "cohort": {"enum": ["SCOUT_SELECTION", "TOP_10_MOVER"]},
        "benchmark_rank": {"type": ["integer", "null"], "minimum": 1, "maximum": 10},
        "execution_result": {"$ref": "#/$defs/executionResult"}
      }
    },

    "policySummary": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "net_realized_pnl_usd", "realized_return_pct", "win_rate_pct",
        "average_winner_pct", "average_loser_pct", "average_capture_ratio",
        "max_drawdown_pct", "exit_reason_counts"
      ],
      "properties": {
        "net_realized_pnl_usd": {"type": "number"},
        "realized_return_pct": {"type": "number"},
        "win_rate_pct": {"type": ["number", "null"], "minimum": 0, "maximum": 100},
        "average_winner_pct": {"type": ["number", "null"]},
        "average_loser_pct": {"type": ["number", "null"]},
        "average_capture_ratio": {"type": ["number", "null"]},
        "max_drawdown_pct": {"type": ["number", "null"]},
        "exit_reason_counts": {"$ref": "#/$defs/exitReasonCounts"}
      }
    },

    "exitReasonCounts": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "TRAILING_STOP", "PROFIT_TARGET", "SESSION_END", "ENTRY_REJECTED"
      ],
      "properties": {
        "TRAILING_STOP": {"type": "integer", "minimum": 0},
        "PROFIT_TARGET": {"type": "integer", "minimum": 0},
        "SESSION_END": {"type": "integer", "minimum": 0},
        "ENTRY_REJECTED": {
          "type": "object",
          "additionalProperties": {"type": "integer", "minimum": 0}
        }
      }
    }
  }
}
